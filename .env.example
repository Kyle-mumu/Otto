# ============================================================
# 环境变量样例 —— API / WS 基址【不在这里配置】
# ------------------------------------------------------------
# 桌面端基址由 vite.config.ts（见 PLATFORM_DEFAULTS）在构建期按 process.platform 决定：
#   macOS   → http://127.0.0.1:8080/api/v1        / ws://127.0.0.1:8080/api/v1
#   Windows → http://121.43.110.135:8000/api/v1   / ws://121.43.110.135:8000/api/v1
# 浏览器（dev / Web 生产）默认走相对路径 /api/v1（Vite proxy 或同源）。
#
# ⚠️ VITE_API_BASE_URL / VITE_WS_BASE_URL 仅在写成【绝对地址】时才生效；
#    相对路径（如 /api/v1）会被显式忽略，不会覆盖平台默认值 —— 以免出现
#    「以为设了其实没生效」。如需临时覆盖，请用绝对地址，例如：
#      VITE_API_BASE_URL=http://192.168.1.10:8000/api/v1 pnpm tauri build
#      VITE_WS_BASE_URL=ws://192.168.1.10:8000/api/v1   pnpm tauri build
# ============================================================
# VITE_API_BASE_URL=http://host:port/api/v1
# VITE_WS_BASE_URL=ws://host:port/api/v1

