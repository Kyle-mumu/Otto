# ============================================================
# Tauri 桌面端专用环境变量（mode=tauri，由 `pnpm run build:tauri` 加载）
# ------------------------------------------------------------
# ⚠️ API / WS 地址【不再】写在本文件里（见 V1.3 批次1 加固 Build-5）：
#    桌面端的 API/WS 基址由 vite.config.ts 在【构建期】按 process.platform 决定：
#      macOS   → http://127.0.0.1:8080/api/v1      （本机 SSH 隧道 / 本地转发）
#      Windows → http://121.43.110.135:8000/api/v1 （直连阿里云 ECS，演示环境）
#    由此保证：
#      · Mac 换机 / 重新 clone 后无需任何本地文件即得正确地址（不再依赖未入库的
#        .env.tauri.local，消除「本地文件缺失 → 静默注入公网地址」的风险）；
#      · Windows 端仍只需 `git pull` + `pnpm tauri build`，无需手动设置变量。
#    如需临时覆盖，请用【绝对地址】显式指定（进程环境变量或 .env.tauri.local）：
#      VITE_API_BASE_URL=http://192.168.1.10:8000/api/v1 pnpm tauri build
#    注意：相对路径（如 /api/v1）会被忽略，不会覆盖平台默认值。
# ============================================================
VITE_APP_VERSION=0.1.23
